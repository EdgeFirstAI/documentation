{% include-markdown "discrete/models/train_vision.md" heading-offset=0 %}

Now that you have trained your model in EdgeFirst Studio, we recommend [converting the trained ONNX model to TensorRT](convert_tensorrt.md) next for optimized performance on the Jetson Orin.

Otherwise, if you prefer deploying the ONNX model instead, you can skip the conversion steps and proceed to [validating the performance of your model](validate/index.md) next.
